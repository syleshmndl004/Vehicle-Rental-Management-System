<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ page_title|default('Vehicle Rental System') }}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="../assets/css/style.css">
</head>
<body class="bg-light">

<nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4">
    <div class="container">
        <a class="navbar-brand" href="{{ base_path }}index.php">🚗 {{ app_name }}</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link" href="{{ base_path }}index.php">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ base_path }}search.php">🔍 Search</a>
                </li>
                {% if session.user_id %}
                    {% if session.is_admin %}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ base_path }}add.php">➕ Add Vehicle</a>
                        </li>
                    {% endif %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ base_path }}bookings.php">
                            📋 {% if session.is_admin %}Bookings{% else %}My Bookings{% endif %}
                        </a>
                    </li>
                {% endif %}
            </ul>
            <div class="d-flex">
                {% if session.user_id %}
                    <span class="navbar-text me-3">
                        Welcome, <strong>{{ session.username }}</strong>
                    </span>
                    <a href="{{ base_path }}logout.php" class="btn btn-outline-light">Logout</a>
                {% else %}
                    <a href="{{ base_path }}login.php" class="btn btn-outline-light me-2">Login</a>
                    <a href="{{ base_path }}register.php" class="btn btn-primary">Sign Up</a>
                {% endif %}
            </div>
        </div>
    </div>
</nav>

<div class="container">
